New:
	y			yank	
	cw			删除光标后一个单词,并进入编辑模式
	g_			go to the last non-blank character of line
	yy			copy the current line
	. 			repeat last command
	N<command>	repeat N times		i.e. 		2dd 删除两行
	N|G| 		去N行	
	w/W		erevious :help next	//W直接跳至n
	e/E 		hereviou selp: next	//E直接跳至:
	%			()[]{}之间的跳转	
	*			向下跳至同一个word	//i.e. 	want xxx	want xxxx
	gu/gU		lowerCase/Upercase
In a Line:
	0		column 0
	^		first character
	g_		last character
	$ 		last column	
	; 后		, 前	
	f->a 	搜索字符a
	t/T->a 	定位在a之前/之后
	--->3fa 	第三个a

复合语句:
	Format:	<star position><command><end position>
	Example:	0y$ means
				0 → go to the beginning of this line
				y → yank from here
				$ → up to the end of this line
	4y$ 	当前光标开始复制四行
	dt"		remove everything till	 "	

File:
	:e				open<path to file>
	:saveas			另存为
	:x,ZZ,:wq		保存并退出
	:q! 				不保存退出


删除:
	x        删除当前光标下的字符
	dw       删除光标之后的单词剩余部分。
	d$       删除光标之后的该行剩余部分。
	yy		复制当前行
	dd       删除当前行
	c        功能和d相同，区别在于完成删除操作后进入INSERT MODE
	cc       也是删除当前行，然后进入INSERT MODE
	删除每行第一个字符    :%s/^.//g
复制黏贴:
	最基本的复制是 y，粘贴是 p，剪切是 d。
	复制和剪切的命令后面都可以接一个移动命令，表示将光标移动区间内的文本复制过来。
	粘贴时 p 与 P 的区别是贴到光标前后的区别。
	如果是在插入模式下想粘贴，也可以不退回普通模式，用 ctrl-r * 来粘贴
	
选中多行:V

系统剪贴板
	"(按住shift)+y
	"+p
	输入模式Shift+Inset

:reg 可以查看各个粘贴板里的内容

真正首位 0
行首  ^
行尾  $

w-e:
	单词首-尾
o:
	开一行进入新行并进入编辑模式
